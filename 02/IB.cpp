#include <cstdio>
#include <iostream>
#include <vector>
using namespace std;
const int maxn = 5e4 + 5;
vector<int> circ[maxn], edge[maxn];
int main() {
  int n, m;
  scanf("%d%d", &n, &m);
  while (m--) {
    int u, v;
    scanf("%d%d", &u, &v);
  }
}